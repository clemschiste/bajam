# session delete
- manual logout (in protected route) -> *OK*
- automated logout cleaner ? -> Every day 1 check of expired sessions and delete them
- manual logout when trying to log in before the cleaner but the session is expired -> In login_user

# tests
- The whole process

README à modifier
docker
